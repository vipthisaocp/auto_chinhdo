# 🗄️ Data Model

> **Project**: Auto EHT  
> **Version**: 1.0

---

## 1. Core Entities

### LDInstance
Đại diện cho một LDPlayer instance.

```
┌─────────────────────────────────────┐
│           LDInstance                │
├─────────────────────────────────────┤
│ + Index: int                        │  // LDPlayer index (0, 1, 2...)
│ + Name: string                      │  // Display name
│ + AdbPort: int                      │  // ADB port (5555, 5557...)
│ + AdbSerial: string                 │  // "localhost:5555"
│ + Status: InstanceStatus            │  // Running, Stopped, Error
│ + IsSelected: bool                  │  // UI checkbox state
│ + IsScriptRunning: bool             │  // Script execution state
│ + CurrentScript: string?            │  // Active script name
└─────────────────────────────────────┘
```

**Enum InstanceStatus:**
- `Detected` - Phát hiện qua ldconsole nhưng chưa verify ADB
- `Connected` - ADB connected thành công
- `Disconnected` - Không connect được
- `Error` - Có lỗi

---

### Template
Hình ảnh dùng để nhận diện.

```
┌─────────────────────────────────────┐
│            Template                 │
├─────────────────────────────────────┤
│ + Id: string (GUID)                 │
│ + Name: string                      │  // User-friendly name
│ + FilePath: string                  │  // Path to image file
│ + Width: int                        │
│ + Height: int                       │
│ + Threshold: double                 │  // Override global (optional)
│ + CreatedAt: DateTime               │
└─────────────────────────────────────┘
```

---

### MatchResult
Kết quả tìm kiếm template.

```
┌─────────────────────────────────────┐
│          MatchResult                │
├─────────────────────────────────────┤
│ + Found: bool                       │
│ + X: int                            │  // Center X of match
│ + Y: int                            │  // Center Y of match
│ + Confidence: double                │  // 0.0 - 1.0
│ + TemplateName: string              │
│ + Timestamp: DateTime               │
└─────────────────────────────────────┘
```

---

### ScriptAction (Abstract)
Base class cho tất cả actions.

```
┌─────────────────────────────────────┐
│       ScriptAction (abstract)       │
├─────────────────────────────────────┤
│ + Type: ActionType                  │
│ + Description: string?              │
│ + Execute(context): Task<bool>      │
└─────────────────────────────────────┘
         ▲
         │
    ┌────┴────┬────────┬────────┬────────┐
    │         │        │        │        │
┌───┴───┐ ┌───┴───┐ ┌──┴──┐ ┌───┴──┐ ┌───┴────┐
│ Click │ │ Wait  │ │Swipe│ │WaitFor│ │Condition│
└───────┘ └───────┘ └─────┘ └──────┘ └────────┘
```

**Enum ActionType:**
- `Click`
- `Wait`
- `WaitFor`
- `Swipe`
- `Condition`
- `Loop`

---

### ClickAction

```
┌─────────────────────────────────────┐
│          ClickAction                │
├─────────────────────────────────────┤
│ + TargetType: ClickTarget           │  // Coordinate or Template
│ + X: int?                           │  // If Coordinate
│ + Y: int?                           │  // If Coordinate
│ + TemplateName: string?             │  // If Template
│ + OffsetX: int                      │  // Offset from center
│ + OffsetY: int                      │
│ + DelayBefore: int                  │  // ms
│ + DelayAfter: int                   │  // ms
└─────────────────────────────────────┘
```

**Enum ClickTarget:**
- `Coordinate` - Click vào tọa độ cố định
- `Template` - Click vào center của template match

---

### WaitAction

```
┌─────────────────────────────────────┐
│          WaitAction                 │
├─────────────────────────────────────┤
│ + DurationMs: int                   │
└─────────────────────────────────────┘
```

---

### WaitForAction

```
┌─────────────────────────────────────┐
│        WaitForAction                │
├─────────────────────────────────────┤
│ + TemplateName: string              │
│ + TimeoutMs: int                    │  // Default 10000
│ + PollIntervalMs: int               │  // Default 500
└─────────────────────────────────────┘
```

---

### SwipeAction

```
┌─────────────────────────────────────┐
│          SwipeAction                │
├─────────────────────────────────────┤
│ + StartX: int                       │
│ + StartY: int                       │
│ + EndX: int                         │
│ + EndY: int                         │
│ + DurationMs: int                   │  // Default 300
└─────────────────────────────────────┘
```

---

### ConditionAction

```
┌─────────────────────────────────────┐
│       ConditionAction               │
├─────────────────────────────────────┤
│ + TemplateName: string              │  // Condition: if template found
│ + ThenActions: List<ScriptAction>   │
│ + ElseActions: List<ScriptAction>   │
└─────────────────────────────────────┘
```

---

### Script

```
┌─────────────────────────────────────┐
│            Script                   │
├─────────────────────────────────────┤
│ + Id: string                        │
│ + Name: string                      │
│ + Description: string?              │
│ + Actions: List<ScriptAction>       │
│ + LoopCount: int                    │  // -1 = infinite
│ + CreatedAt: DateTime               │
│ + UpdatedAt: DateTime               │
└─────────────────────────────────────┘
```

---

### LogEntry

```
┌─────────────────────────────────────┐
│           LogEntry                  │
├─────────────────────────────────────┤
│ + Timestamp: DateTime               │
│ + Level: LogLevel                   │  // Info, Warning, Error
│ + InstanceName: string              │
│ + Message: string                   │
│ + ActionType: ActionType?           │
└─────────────────────────────────────┘
```

---

### AppSettings

```
┌─────────────────────────────────────┐
│          AppSettings                │
├─────────────────────────────────────┤
│ + DefaultThreshold: double          │  // Default 0.8
│ + DefaultClickDelay: int            │  // ms, default 100
│ + AdbPath: string?                  │  // Override ADB path
│ + LDConsolePath: string?            │  // Override ldconsole path
│ + TemplatesFolder: string           │  // Default "./templates"
│ + ScriptsFolder: string             │  // Default "./scripts"
└─────────────────────────────────────┘
```

---

## 2. File Structure

```
📁 Auto_EHT/
├── 📁 src/
│   ├── 📁 Models/           ← Data classes
│   ├── 📁 Services/         ← Business logic
│   ├── 📁 ViewModels/       ← MVVM ViewModels
│   └── 📁 Views/            ← WPF Views
├── 📁 templates/            ← Template images
├── 📁 scripts/              ← Script JSON files
└── appsettings.json         ← App configuration
```

---

## 3. Persistence

| Data | Storage | Format |
|------|---------|--------|
| Templates metadata | `templates/index.json` | JSON |
| Scripts | `scripts/{name}.json` | JSON |
| Settings | `appsettings.json` | JSON |
| Logs | In-memory (session only) | - |
