<Window x:Class="AdminPanel.Views.AdminLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập Admin - Quản lý License" 
        Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#0a0e27">
    
    <Grid>
        <Border Background="#0f1429" 
                CornerRadius="15" 
                Margin="20"
                Padding="30"
                BorderBrush="#1a1f3a"
                BorderThickness="1">
            <StackPanel VerticalAlignment="Center">
                <!-- Title -->
                <TextBlock Text="🔒 ADMIN LOGIN" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#6366f1"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5"/>
                
                <TextBlock Text="Vui lòng xác thực quyền quản trị" 
                           FontSize="13" 
                           Foreground="#94a3b8"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>
                
                <!-- Account -->
                <TextBlock Text="Tài khoản admin" 
                           Foreground="#e2e8f0" 
                           FontSize="12"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="TxtUsername" 
                         Height="45" 
                         FontSize="14"
                         Padding="12,10"
                         Background="#1e293b"
                         Foreground="White"
                         BorderBrush="#334155"
                         BorderThickness="1"
                         VerticalContentAlignment="Center"/>
                
                <!-- Password -->
                <TextBlock Text="Mật khẩu" 
                           Foreground="#e2e8f0" 
                           FontSize="12"
                           FontWeight="SemiBold"
                           Margin="0,20,0,8"/>
                <PasswordBox x:Name="TxtPassword" 
                             Height="45" 
                             FontSize="14"
                             Padding="12,10"
                             Background="#1e293b"
                             Foreground="White"
                             BorderBrush="#334155"
                             BorderThickness="1"
                             VerticalContentAlignment="Center"/>
                
                <!-- Error Message -->
                <TextBlock x:Name="TxtError" 
                           Foreground="#ef4444" 
                           FontSize="12"
                           Margin="0,15,0,0"
                           TextWrapping="Wrap"
                           Visibility="Collapsed"/>
                
                <!-- Login Button -->
                <Button x:Name="BtnLogin" 
                        Content="Đăng Nhập Quản Trị" 
                        Height="48"
                        FontSize="15"
                        FontWeight="Bold"
                        Background="#6366f1"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,25,0,0"
                        Click="BtnLogin_Click"/>
                
                <!-- Loading -->
                <TextBlock x:Name="TxtLoading" 
                           Text="Đang xác thực..." 
                           Foreground="#6366f1"
                           FontSize="13"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"
                           Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
